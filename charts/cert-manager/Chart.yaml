apiVersion: v2
name: cert-manager
description: cert-manager for TLS certificate management in homelab Kubernetes cluster
type: application
version: 1.0.0
appVersion: "v1.15.3"

dependencies:
  - name: cert-manager
    version: v1.15.3
    repository: https://charts.jetstack.io
# Test deploy workflow
