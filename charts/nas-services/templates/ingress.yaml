{{- range $name, $config := .Values.services }}
{{- if $config.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $name }}
  namespace: {{ $.Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ $name }}
    app.kubernetes.io/instance: {{ $.Release.Name }}
    app.kubernetes.io/managed-by: {{ $.Release.Service }}
  annotations:
    {{- if $config.useAuthentikProxy }}
    {{- if $config.proxyAuthHeaders }}
    # Forward auth mode with Authorization header passing
    nginx.ingress.kubernetes.io/enable-external-auth-annotations: "true"
    nginx.ingress.kubernetes.io/auth-url: http://authentik-outpost.default.svc.cluster.local:9000/outpost.goauthentik.io/auth/nginx
    nginx.ingress.kubernetes.io/auth-signin: https://auth.{{ $.Values.baseDomain }}/outpost.goauthentik.io/start?rd=$scheme://$http_host$escaped_request_uri
    nginx.ingress.kubernetes.io/auth-response-headers: Set-Cookie,X-authentik-username,X-authentik-groups,X-authentik-email,X-authentik-name,X-authentik-uid,Authorization
    nginx.ingress.kubernetes.io/auth-snippet: |
      auth_request_set $authentik_username $upstream_http_x_authentik_username;
      auth_request_set $authentik_groups $upstream_http_x_authentik_groups;
      auth_request_set $authentik_email $upstream_http_x_authentik_email;
      auth_request_set $authentik_name $upstream_http_x_authentik_name;
      auth_request_set $authentik_uid $upstream_http_x_authentik_uid;
      auth_request_set $authentik_auth $upstream_http_authorization;
      
      proxy_set_header X-authentik-username $authentik_username;
      proxy_set_header X-authentik-groups $authentik_groups;
      proxy_set_header X-authentik-email $authentik_email;
      proxy_set_header X-authentik-name $authentik_name;
      proxy_set_header X-authentik-uid $authentik_uid;
      proxy_set_header Authorization $authentik_auth;
    {{- end }}
    {{- end }}
    {{- if $config.annotations }}
    {{- toYaml $config.annotations | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: {{ $.Values.ingressClassName }}
  tls:
    - hosts:
        - {{ $config.subdomain }}.{{ $.Values.baseDomain }}
      secretName: {{ $.Values.tlsSecretName }}
  rules:
    - host: {{ $config.subdomain }}.{{ $.Values.baseDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                {{- if and $config.useAuthentikProxy $config.proxyAuthHeaders }}
                # Forward auth mode: route directly to NAS service
                name: {{ $name }}-nas
                port:
                  number: {{ $config.port }}
                {{- else if $config.useAuthentikProxy }}
                # Proxy mode: route through authentik-outpost
                name: authentik-outpost
                port:
                  number: 9000
                {{- else }}
                name: {{ $name }}
                port:
                  number: {{ $config.port }}
                {{- end }}
{{- end }}
{{- end }}
