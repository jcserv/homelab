{{- /*
InfisicalSecret CR. When .Values.infisical.enabled is true, the operator pulls
secrets from the self-hosted Infisical platform (folder /authentik) and
materializes both the `authentik-secrets` and `authentik-outpost-token` Secrets,
replacing the SealedSecrets in sealed-secrets/.
See docs/runbooks/INFISICAL_MIGRATION.md.
*/ -}}
{{- if .Values.infisical.enabled }}
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: authentik-infisical
  labels:
    app: {{ include "authentik.name" . }}
spec:
  hostAPI: {{ .Values.infisical.hostAPI | quote }}
  syncConfig:
    resyncInterval: {{ .Values.infisical.resyncInterval | quote }}
    instantUpdates: {{ .Values.infisical.instantUpdates }}
  authentication:
    universalAuth:
      secretsScope:
        projectSlug: {{ .Values.infisical.projectSlug | quote }}
        envSlug: {{ .Values.infisical.envSlug | quote }}
        secretsPath: {{ .Values.infisical.secretsPath | quote }}
      credentialsRef:
        secretName: {{ .Values.infisical.credentialsRef.secretName | quote }}
        secretNamespace: {{ .Values.infisical.credentialsRef.secretNamespace | quote }}
  managedKubeSecretReferences:
    - secretName: authentik-secrets
      secretNamespace: {{ .Release.Namespace }}
      creationPolicy: "Owner"
      secretType: Opaque
      template:
        includeAllSecrets: false
        data:
          AUTHENTIK_POSTGRESQL__PASSWORD: "{{ `{{ .AUTHENTIK_POSTGRESQL__PASSWORD.Value }}` }}"
          AUTHENTIK_POSTGRESQL__USER: "{{ `{{ .AUTHENTIK_POSTGRESQL__USER.Value }}` }}"
          AUTHENTIK_SECRET_KEY: "{{ `{{ .AUTHENTIK_SECRET_KEY.Value }}` }}"
    - secretName: authentik-outpost-token
      secretNamespace: {{ .Release.Namespace }}
      creationPolicy: "Owner"
      secretType: Opaque
      template:
        includeAllSecrets: false
        data:
          token: "{{ `{{ .token.Value }}` }}"
{{- end }}
