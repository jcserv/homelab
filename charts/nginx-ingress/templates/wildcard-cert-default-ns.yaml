---
# Wildcard certificate for *.jarrodservilla.com domain in default namespace
# This allows all ingresses in default namespace to share the same wildcard cert
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-jarrodservilla-cert-default
  namespace: default
spec:
  secretName: wildcard-jarrodservilla-tls
  duration: 2160h # 90 days (Let's Encrypt default)
  renewBefore: 720h # 30 days
  commonName: "*.jarrodservilla.com"
  dnsNames:
    - "*.jarrodservilla.com"
    - "jarrodservilla.com"
  privateKey:
    algorithm: RSA
    size: 2048
  issuerRef:
    name: cloudflare-issuer
    kind: ClusterIssuer
    group: cert-manager.io
